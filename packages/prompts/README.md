# prompts

shared prompt files in markdown. backend reads them with `open()`. frontend
imports them as raw text via the next.js raw loader when previewing.

each file is named `<agent>.md` and follows the same shape: who you are,
how you work, what you never do.
